/**
 * @format
 */

import { AppRegistry } from 'react-native';
import NavigationView from './Views/NavigationView'
import { name as appName } from './app.json';

AppRegistry.registerComponent(appName, () => NavigationView);
